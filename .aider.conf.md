# FairArena - Curl Tester — Aider AI Context

You are working on **FairArena - Curl Tester**, a small web app that provides
sandboxed Docker terminals, a cURL/API request tester, and a real-time webhook
inspector for learning, debugging, and demos.

## Quick Facts

**Tech:** React 19 + TypeScript (client), Node 22 + TypeScript + Express (server)
**Status:** Development

## Stack Details

- **Frontend:** React 19, TypeScript, Vite, TailwindCSS
- **Backend:** Node 22, Express, node-pty, Docker-managed sandboxes
- **Optional:** Upstash Redis for per-IP session claims

## Code Standards

- **TypeScript:** Strict mode; prefer explicit types and `unknown` over `any`
- **React:** Functional components and hooks; keep components small and testable
- **API:** Small controller routes; validation via `zod` where appropriate

## Environment Variables (common)

- `PORT` — HTTP port for the server (default: `4000`)
- `VITE_API_URL` — Frontend API URL for Vercel/prod
- `ALLOWED_ORIGINS` — CORS allow-list (comma separated)
- `UPSTASH_REDIS_URL` / `UPSTASH_REDIS_TOKEN` — optional Upstash credentials

Keep this file minimal — Aider can use these facts when generating code or
helping with tasks in the repo.
